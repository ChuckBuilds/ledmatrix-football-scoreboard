{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Football Scoreboard Plugin Configuration",
  "description": "Configuration schema for the Football Scoreboard plugin - displays live, recent, and upcoming NFL and NCAA Football games",
  "type": "object",
  "properties": {
    "enabled": {
      "type": "boolean",
      "default": true,
      "description": "Enable or disable the football scoreboard plugin"
    },
    "display_duration": {
      "type": "number",
      "default": 30,
      "minimum": 5,
      "maximum": 300,
      "description": "Duration in seconds for the display controller to show this plugin mode before rotating to next plugin"
    },
    "live_priority": {
      "type": "boolean",
      "default": false,
      "description": "Enable live priority takeover when plugin has live content"
    },
    "high_performance_transitions": {
      "type": "boolean",
      "default": false,
      "description": "Use high-performance transitions (120 FPS) instead of standard (30 FPS)"
    },
    "update_interval": {
      "type": "integer",
      "default": 3600,
      "minimum": 30,
      "maximum": 86400,
      "description": "How often to fetch new data in seconds"
    },
    "transition": {
      "type": "object",
      "description": "Transition configuration for this plugin",
      "properties": {
        "type": {
          "type": "string",
          "enum": ["redraw", "fade", "slide", "wipe", "dissolve", "pixelate"],
          "default": "redraw",
          "description": "Type of transition to use"
        },
        "speed": {
          "type": "integer",
          "default": 2,
          "minimum": 1,
          "maximum": 10,
          "description": "Transition speed (1=slow, 10=fast)"
        },
        "enabled": {
          "type": "boolean",
          "default": true,
          "description": "Enable or disable transitions"
        }
      },
      "additionalProperties": false
    },
    "game_display_duration": {
      "type": "number",
      "default": 15,
      "minimum": 3,
      "maximum": 60,
      "description": "Duration in seconds to show each individual game before rotating to the next game within the same mode"
    },
    "dynamic_duration": {
      "type": "object",
      "title": "Dynamic Duration Settings",
      "description": "Control whether the plugin shows all games before moving to the next mode. Can be configured globally or per-league/per-mode for granular control.",
      "properties": {
        "enabled": {
          "type": "boolean",
          "default": false,
          "description": "Global enable for dynamic duration. When enabled, the plugin will show each game once before moving to the next mode, overriding the normal display_duration setting. Can be overridden by per-league or per-mode settings."
        },
        "max_duration_seconds": {
          "type": "number",
          "default": 300,
          "minimum": 60,
          "maximum": 600,
          "description": "Global maximum time in seconds to wait for all games to be displayed. Can be overridden by per-league settings."
        },
        "modes": {
          "type": "object",
          "title": "Per-Mode Settings",
          "description": "Configure dynamic duration for specific modes (live, recent, upcoming). These override global settings.",
          "properties": {
            "live": {
              "type": "object",
              "properties": {
                "enabled": {
                  "type": "boolean",
                  "default": false,
                  "description": "Enable dynamic duration for live games"
                },
                "max_duration_seconds": {
                  "type": "number",
                  "minimum": 60,
                  "maximum": 600,
                  "description": "Max duration for live games (overrides global)"
                }
              }
            },
            "recent": {
              "type": "object",
              "properties": {
                "enabled": {
                  "type": "boolean",
                  "default": false,
                  "description": "Enable dynamic duration for recent games"
                },
                "max_duration_seconds": {
                  "type": "number",
                  "minimum": 60,
                  "maximum": 600,
                  "description": "Max duration for recent games (overrides global)"
                }
              }
            },
            "upcoming": {
              "type": "object",
              "properties": {
                "enabled": {
                  "type": "boolean",
                  "default": false,
                  "description": "Enable dynamic duration for upcoming games"
                },
                "max_duration_seconds": {
                  "type": "number",
                  "minimum": 60,
                  "maximum": 600,
                  "description": "Max duration for upcoming games (overrides global)"
                }
              }
            }
          }
        }
      }
    },
    "test_mode": {
      "type": "boolean",
      "default": false,
      "description": "Enable test mode with sample games"
    },
    "nfl": {
      "type": "object",
      "title": "NFL Settings",
      "description": "Configuration for NFL games",
      "properties": {
        "enabled": {
          "type": "boolean",
          "default": true,
          "description": "Enable NFL games"
        },
        "favorite_teams": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [],
          "description": "List of favorite NFL team abbreviations (e.g., TB, DAL, GB). Use 2-3 letter codes."
        },
        "display_modes": {
          "type": "object",
          "title": "Display Modes",
          "description": "Control which game types to show",
          "properties": {
            "show_live": {
              "type": "boolean",
              "default": true,
              "description": "Show live NFL games"
            },
            "show_recent": {
              "type": "boolean",
              "default": true,
              "description": "Show recently completed NFL games"
            },
            "show_upcoming": {
              "type": "boolean",
              "default": true,
              "description": "Show upcoming NFL games"
            }
          }
        },
        "live_priority": {
          "type": "boolean",
          "default": true,
          "description": "Give live games priority over other modes. When enabled, live games will interrupt the normal mode rotation and be displayed immediately when available."
        },
        "live_game_duration": {
          "type": "integer",
          "default": 30,
          "minimum": 10,
          "maximum": 120,
          "description": "Duration in seconds to display each live game before rotating to the next live game"
        },
        "live_update_interval": {
          "type": "integer",
          "default": 30,
          "minimum": 5,
          "maximum": 300,
          "description": "How often to update live game data (seconds)"
        },
        "game_limits": {
          "type": "object",
          "title": "Game Limits",
          "description": "Control how many games to show",
          "properties": {
            "recent_games_to_show": {
              "type": "integer",
              "default": 5,
              "minimum": 1,
              "maximum": 20,
              "description": "With favorite teams: Number of recent games PER team (e.g., 1 with 2 teams = 2 games). Without favorite teams: Total number of most recent games to show (e.g., 5 = show 5 most recent games total)."
            },
            "upcoming_games_to_show": {
              "type": "integer",
              "default": 1,
              "minimum": 1,
              "maximum": 20,
              "description": "With favorite teams: Number of upcoming games PER team (e.g., 2 with 3 teams = up to 6 games). Without favorite teams: Total number of next upcoming games to show (e.g., 1 = show only the next 1 game total)."
            }
          }
        },
        "display_options": {
          "type": "object",
          "title": "Display Options",
          "description": "Additional information to show",
          "properties": {
            "show_records": {
              "type": "boolean",
              "default": false,
              "description": "Show team records (wins-losses)"
            },
            "show_ranking": {
              "type": "boolean",
              "default": false,
              "description": "Show team rankings (when available)"
            },
            "show_odds": {
              "type": "boolean",
              "default": true,
              "description": "Show betting odds"
            }
          }
        },
        "filtering": {
          "type": "object",
          "title": "Filtering Options",
          "description": "Control which teams are shown",
          "properties": {
            "show_favorite_teams_only": {
              "type": "boolean",
              "default": true,
              "description": "Only show games from favorite teams"
            },
            "show_all_live": {
              "type": "boolean",
              "default": false,
              "description": "Show all live games, not just favorites"
            }
          }
        },
        "dynamic_duration": {
          "type": "object",
          "title": "NFL Dynamic Duration Settings",
          "description": "Override global dynamic duration settings for NFL games. If not set, uses global settings.",
          "properties": {
            "enabled": {
              "type": "boolean",
              "default": false,
              "description": "Enable dynamic duration for NFL games (overrides global setting)"
            },
            "max_duration_seconds": {
              "type": "number",
              "minimum": 60,
              "maximum": 600,
              "description": "Max duration for NFL games (overrides global setting)"
            },
            "modes": {
              "type": "object",
              "title": "Per-Mode Settings for NFL",
              "description": "Configure dynamic duration for specific NFL modes",
              "properties": {
                "live": {
                  "type": "object",
                  "properties": {
                    "enabled": {
                      "type": "boolean",
                      "default": false,
                      "description": "Enable dynamic duration for NFL live games"
                    },
                    "max_duration_seconds": {
                      "type": "number",
                      "minimum": 60,
                      "maximum": 600,
                      "description": "Max duration for NFL live games"
                    }
                  }
                },
                "recent": {
                  "type": "object",
                  "properties": {
                    "enabled": {
                      "type": "boolean",
                      "default": false,
                      "description": "Enable dynamic duration for NFL recent games"
                    },
                    "max_duration_seconds": {
                      "type": "number",
                      "minimum": 60,
                      "maximum": 600,
                      "description": "Max duration for NFL recent games"
                    }
                  }
                },
                "upcoming": {
                  "type": "object",
                  "properties": {
                    "enabled": {
                      "type": "boolean",
                      "default": false,
                      "description": "Enable dynamic duration for NFL upcoming games"
                    },
                    "max_duration_seconds": {
                      "type": "number",
                      "minimum": 60,
                      "maximum": 600,
                      "description": "Max duration for NFL upcoming games"
                    }
                  }
                }
              }
            }
          }
        }
      }
    },
    "ncaa_fb": {
      "type": "object",
      "title": "NCAA Football Settings",
      "description": "Configuration for NCAA Football games",
      "properties": {
        "enabled": {
          "type": "boolean",
          "default": false,
          "description": "Enable NCAA Football games"
        },
        "favorite_teams": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [],
          "description": "List of favorite NCAA FB team abbreviations (e.g., UGA, AUB, BAMA). Use 2-4 letter codes."
        },
        "display_modes": {
          "type": "object",
          "title": "Display Modes",
          "description": "Control which game types to show",
          "properties": {
            "show_live": {
              "type": "boolean",
              "default": true,
              "description": "Show live NCAA FB games"
            },
            "show_recent": {
              "type": "boolean",
              "default": true,
              "description": "Show recently completed NCAA FB games"
            },
            "show_upcoming": {
              "type": "boolean",
              "default": true,
              "description": "Show upcoming NCAA FB games"
            }
          }
        },
        "live_priority": {
          "type": "boolean",
          "default": true,
          "description": "Give live games priority over other modes. When enabled, live games will interrupt the normal mode rotation and be displayed immediately when available."
        },
        "live_game_duration": {
          "type": "integer",
          "default": 20,
          "minimum": 10,
          "maximum": 120,
          "description": "Duration in seconds to display each live game before rotating to the next live game"
        },
        "live_update_interval": {
          "type": "integer",
          "default": 30,
          "minimum": 5,
          "maximum": 300,
          "description": "How often to update live game data (seconds)"
        },
        "game_limits": {
          "type": "object",
          "title": "Game Limits",
          "description": "Control how many games to show",
          "properties": {
            "recent_games_to_show": {
              "type": "integer",
              "default": 1,
              "minimum": 1,
              "maximum": 20,
              "description": "With favorite teams: Number of recent games PER team (e.g., 1 with 2 teams = 2 games). Without favorite teams: Total number of most recent games to show (e.g., 5 = show 5 most recent games total)."
            },
            "upcoming_games_to_show": {
              "type": "integer",
              "default": 1,
              "minimum": 1,
              "maximum": 20,
              "description": "With favorite teams: Number of upcoming games PER team (e.g., 2 with 3 teams = up to 6 games). Without favorite teams: Total number of next upcoming games to show (e.g., 1 = show only the next 1 game total)."
            }
          }
        },
        "display_options": {
          "type": "object",
          "title": "Display Options",
          "description": "Additional information to show",
          "properties": {
            "show_records": {
              "type": "boolean",
              "default": false,
              "description": "Show team records (wins-losses)"
            },
            "show_ranking": {
              "type": "boolean",
              "default": true,
              "description": "Show team rankings (rankings are very important in college football)"
            },
            "show_odds": {
              "type": "boolean",
              "default": true,
              "description": "Show betting odds"
            }
          }
        },
        "filtering": {
          "type": "object",
          "title": "Filtering Options",
          "description": "Control which teams are shown",
          "properties": {
            "show_favorite_teams_only": {
              "type": "boolean",
              "default": true,
              "description": "Only show games from favorite teams"
            },
            "show_all_live": {
              "type": "boolean",
              "default": false,
              "description": "Show all live games, not just favorites"
            }
          }
        },
        "dynamic_duration": {
          "type": "object",
          "title": "NCAA FB Dynamic Duration Settings",
          "description": "Override global dynamic duration settings for NCAA FB games. If not set, uses global settings.",
          "properties": {
            "enabled": {
              "type": "boolean",
              "default": false,
              "description": "Enable dynamic duration for NCAA FB games (overrides global setting)"
            },
            "max_duration_seconds": {
              "type": "number",
              "minimum": 60,
              "maximum": 600,
              "description": "Max duration for NCAA FB games (overrides global setting)"
            },
            "modes": {
              "type": "object",
              "title": "Per-Mode Settings for NCAA FB",
              "description": "Configure dynamic duration for specific NCAA FB modes",
              "properties": {
                "live": {
                  "type": "object",
                  "properties": {
                    "enabled": {
                      "type": "boolean",
                      "default": false,
                      "description": "Enable dynamic duration for NCAA FB live games"
                    },
                    "max_duration_seconds": {
                      "type": "number",
                      "minimum": 60,
                      "maximum": 600,
                      "description": "Max duration for NCAA FB live games"
                    }
                  }
                },
                "recent": {
                  "type": "object",
                  "properties": {
                    "enabled": {
                      "type": "boolean",
                      "default": false,
                      "description": "Enable dynamic duration for NCAA FB recent games"
                    },
                    "max_duration_seconds": {
                      "type": "number",
                      "minimum": 60,
                      "maximum": 600,
                      "description": "Max duration for NCAA FB recent games"
                    }
                  }
                },
                "upcoming": {
                  "type": "object",
                  "properties": {
                    "enabled": {
                      "type": "boolean",
                      "default": false,
                      "description": "Enable dynamic duration for NCAA FB upcoming games"
                    },
                    "max_duration_seconds": {
                      "type": "number",
                      "minimum": 60,
                      "maximum": 600,
                      "description": "Max duration for NCAA FB upcoming games"
                    }
                  }
                }
              }
            }
          }
        }
      }
    }
  },
  "additionalProperties": false,
  "required": ["enabled"]
}

